OPENAI_API_KEY=sk-proj-...
EMBEDDING_MODEL=text-embedding-3-small
LLM_MODEL=gpt-4o-mini

# Необязательно: защита API ключом (передавать в заголовке X-API-Key)
API_KEY=

# Необязательно: разрешённые origins через запятую (по умолчанию *)
# ALLOWED_ORIGINS=https://site.com,https://www.site.com
